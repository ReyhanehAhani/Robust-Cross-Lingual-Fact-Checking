# Published labeling deliverable

| File | Purpose |
|------|---------|
| `demo_annotations.json` | **Completed** annotation payload (claim → verdict) in Label Studio–compatible JSON — same schema you get from **Export** after labeling. |

Linked setup files live in `label_studio/config/` and `label_studio/sample_tasks/`.

To view in Label Studio: import this file via **Import** (or merge with a fresh project using the same labeling config).
